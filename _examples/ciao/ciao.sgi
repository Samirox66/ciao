TERMINALS:
    number ::= '[0-9]+(.[0-9]*)?';
    name ::= '[\w\D][\w]*';
    string ::= '"(\\.|[^\\"]+)"';
    code ::= '\{[^\}]*\}';
    char_key ::= '/|\(|\)|,|;|\.|:|=|\->'.

KEYS:
    'VAR'; 'EFFECT'; 'EVENT'; 'INNER';  'STATE';
    'else'; 'false'; 'true'; '/'; '('; ')'; ','; '.'; ':'; '->'; '='.

NONTERMINALS:
    CIAO;
    AUTOMATA_OBJECT;
    VAR_BLOCK;
    EFFECT_BLOCK;
    EVENT_BLOCK;
    INNER_BLOCK;
    FUNCTION;
    STATE_BLOCK;
    TRANSITION_DESCRIPTION.

AXIOM: CIAO.
