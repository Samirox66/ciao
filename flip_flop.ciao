FlipFlop

VAR:
    i = 5.0;
    k = "ON";
    test = true;
    button = false.

EFFECT:
    isItLightOutside(arg);
    turnOnLamp();
    turnOffLamp().

EVENT:
    buttonPress(mode);
    isButtonPressed().


STATE:
    ready -> {buttonPress(mode)} / {self.button} /  -> check;
   
